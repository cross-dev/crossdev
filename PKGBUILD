pkgname=crossdev
pkgver=1
pkgrel=1
arch=('any')
license=('GPL')
depends=('kconfig-frontends'
         'make'
         'grep'
         'fakeroot'
         'file')

package() {
    : Absolutely nothing to do
}

