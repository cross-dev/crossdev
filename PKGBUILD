pkgname=crossdev
pkgver=1
pkgrel=2
arch=('any')
license=('GPL')
depends=('kconfig-frontends'
         'make'
         'grep'
         'fakeroot'
         'pkg-config'
         'file')

package() {
    : Absolutely nothing to do
}

